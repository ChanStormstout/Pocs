# De..set ALIGN,    1<<0     obits
st# ies
.set MEMINFO,  1<<1 th
	# r
	.set FLAGS,    ALIGN | MEMINFO  'fld
.set MAGIC,    0x1BADB002       # er
.set CHECKSUM, -(MAGIC + FLAGS) #.
.section .multiboot
.align 4
.long MAGIC
.long FLAGS
.long CHECKSUM

#.
.section .bootstrap_stiso/boot/bzImage
/out/
/*.zipack, "ÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖÖ 16384# 16 KiB
stÕcklobal _start
.type _start, @functioN
_start:
	# We to
	# lg
	#e
	#t
	;nt:
	# ye
	#
	# .

	# Byme
	# th in
	# to `f
	# s
	# uff
	# s.

	# Toof
	# ou
	movl $stack_toŽ, %esp

	#  an
	# a
	# .
	call kernel_main

	# 
	# n
	#il
	#         
#y.ÿcli
	hlt
.Lha r
	.set FLAGS,    ALIGNÿÿÿÿÿÿ#g.
.size _start, . - _start
